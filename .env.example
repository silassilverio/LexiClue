# Backend environment variables (used by backend/src/main.rs via dotenvy).
# Copy this file to .env.local and fill in your values.

# Required: Groq API key — get yours free at https://console.groq.com/keys
# Much higher free-tier limits than Gemini.
GROQ_API_KEY="YOUR_GROQ_API_KEY"
